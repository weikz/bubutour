<%= render "common/navbar" %>

<div class="container">
  <div class="row well">
    <div class="col-md-12">
      <h2>修改旅遊資訊</h2>
      <%= simple_form_for(@tour) do |f| %>
        <%= f.input :title %>
        <%= f.input :description, as: :text %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>

<%= render "common/footer" %>
